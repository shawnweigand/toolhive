# Example values.yaml using portfolio data
deployment:
  replicas: 1
  image:
    repository: ghcr.io/stacklok/toolhive-cloud-ui
    tag: v0.5.1

ingress:
  host: toolhive.example.com
  tls: true
  externalPort: 80
  internalPort: 5678

# secret:
#   dopplerToken: overwriteThisValue

issuer:
  email: user@email.com
  server: https://acme-v02.api.letsencrypt.org/directory

# config:
#   APP_NAME: test
#   REDIS_HOST: test-redis